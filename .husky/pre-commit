#!/bin/sh
set -e

[ -n "$CI" ] && exit 0
npx lint-staged --config ./.husky/lint-staged.config.js
node scripts/publication-safety/scan-staged.mjs
node scripts/publication-safety/mark-reviewed.mjs --check
